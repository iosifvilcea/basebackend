# TODO

- [ ] Add database integration tests that exercise repositories against a real PostgreSQL instance, so schema issues (e.g. missing tables, constraint violations) are caught in CI before reaching production.
